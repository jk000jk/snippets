// lab 9

// 1
#include <stdio.h>


int main()
{
	int n, ip = 0, i, aux, ip2 = 0;
	
	printf("n = ");
	scanf("%d", &n);
	
	while(n > 0){	
      aux = n % 10; 
      if(aux % 2 != 0) {
		   ip = ip * 10 + aux;
	  }	  
	  n /= 10;
	}
	
	while(ip > 0){
		aux = ip % 10;
		ip2 = ip2 * 10 + aux;
		ip /= 10;
	}
	
	printf("%d", ip2);
	return 0;
}

// 2

#include <stdio.h>

int sumad (int n)
{
   int s, d;
   s = n + 1;

   for(d = 2; d <= n/2; d++)
	   if (n%d == 0) s = s + d;
   
   return s;
}


int main2()
{
	int i, a, b;
	
	printf("a = ");
	scanf("%d", &a);
	
	printf("b = ");
	scanf("%d", &b);
	
	printf("Nr. prime cuprinse intre %d si %d : \n", a, b);
	for(i = a; i <= b; i++)
		if(sumad(i) == i + 1)
			printf("%d ", i);
	
	return 0;
}
